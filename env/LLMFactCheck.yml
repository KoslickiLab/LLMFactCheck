name: myLLMFactCheck
channels:
  - defaults
dependencies:
  - python
  - pip
  - pip:

    - pandas
    - numpy
    - matplotlib
    - pytest
    - pytest-cov
    - pytest-mock
    - cudatoolkit
    - llama-cpp-python
    - huggingface-hub
   
