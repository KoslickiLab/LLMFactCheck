name: myLLMFactCheck
channels:
  - conda-forge
  - defaults
dependencies:
  - python==3.1
  - pandas
  - numpy
  - scikit-learn
  - matplotlib
  - pytest
  - pytest-cov
  - pytest-mock
  - pip
  - nvidia::cuda-toolkit
  - pip:
    - llama-cpp-python --force-reinstall --upgrade --no-cache-dir --verbose
    - huggingface-hub
