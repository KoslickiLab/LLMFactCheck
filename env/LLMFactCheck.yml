name: myLLMFactCheck
channels:
  - nvidia
  - defaults
dependencies:
  - python
  - pandas
  - numpy
  - matplotlib
  - pytest
  - pytest-cov
  - pytest-mock
  - cuda-toolkit
  - pip
  - pip:
    - llama-cpp-python 
    - huggingface-hub
   
