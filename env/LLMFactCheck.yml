name: myLLMFactCheck
channels:
  - nvidia
  - defaults
dependencies:
  - python
  - cuda-toolkit
  - pip
  - pip:
    - pandas
    - numpy
    - matplotlib
    - pytest
    - pytest-cov
    - pytest-mock
    - llama-cpp-python 
    - huggingface-hub
    - openai
   
