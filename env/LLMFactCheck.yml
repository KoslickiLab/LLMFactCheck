name: myLLMFactCheck
channels:
  - conda-forge
  - defaults
dependencies:
  - pip
  - pip:
    - python
    - pandas
    - numpy
    - matplotlib
    - pytest
    - pytest-cov
    - pytest-mock
    - cudatoolkit
    - llama-cpp-python --force-reinstall --upgrade --no-cache-dir --verbose
    - huggingface-hub
   
