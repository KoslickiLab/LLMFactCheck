name: myLLMFactCheck
channels:
  - nvidia
  - defaults
dependencies:
  - cuda-toolkit
  - pip
  - pip:
    - python
    - pandas
    - numpy
    - matplotlib
    - pytest
    - pytest-cov
    - pytest-mock
    - llama-cpp-python 
    - huggingface-hub
   
