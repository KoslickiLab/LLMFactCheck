name: myLLMFactCheck
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pandas
  - numpy
  - matplotlib
  - pytest
  - pytest-cov
  - pytest-mock
  - pip
  - pip:
    - cudatoolkit
    - llama-cpp-python --force-reinstall --upgrade --no-cache-dir --verbose
    - huggingface-hub
   
